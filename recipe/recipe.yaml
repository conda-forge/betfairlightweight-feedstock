schema_version: 1

context:
  name: betfairlightweight
  version: "2.23.2"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: 44439f5bb67a9f6344ef70d7a14af2e4886b39a113e6295fbf734c6384eb1c3c

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-build-isolation --no-deps

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling
  run:
    - python >=${{ python_min }}
    - requests <2.33.0
    - orjson >=3.11.4
    - ciso8601 >=2.3.3

tests:
  - python:
      imports:
        - betfairlightweight
        - betfairlightweight.endpoints
        - betfairlightweight.resources
        - betfairlightweight.streaming
      pip_check: true
      python_version: ${{ python_min }}.*
  - files:
      source:
        - tests/*
        - HISTORY.rst
    requirements:
      run:
        - mock
        - python ${{ python_min }}.*
    script:
      - python -m unittest discover -s tests --verbose

about:
  license: MIT
  license_file: LICENSE
  summary: Lightweight python wrapper for Betfair API-NG
  description: |
    Lightweight, super fast pythonic wrapper for Betfair API-NG allowing all
    betting operations (including market and order streaming) and most account operations.
  homepage: https://github.com/betcode-org/betfair
  repository: https://github.com/betcode-org/betfair

extra:
  recipe-maintainers:
    - synapticarbors
    - liampauling
